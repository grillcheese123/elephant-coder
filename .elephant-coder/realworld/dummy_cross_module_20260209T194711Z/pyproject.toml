[project]
name = "dummy-cross-module-project"
version = "0.1.0"
description = "Synthetic cross-module inheritance project for Elephant benchmark scenarios."
requires-python = ">=3.12"

[tool.pytest.ini_options]
addopts = "-q"

